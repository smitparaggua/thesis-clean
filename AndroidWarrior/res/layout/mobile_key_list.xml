<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="fill_parent"
                android:layout_height="?android:attr/listPreferredItemHeight"
                android:padding="6dip" >

    <ImageButton
        android:id="@+id/upload_button"
        android:layout_width="?android:attr/listPreferredItemHeight"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:contentDescription="TODO"
        android:scaleType="fitCenter"
        android:background="@color/black"
        android:src="@drawable/upload" />

    <TextView
        android:id="@+id/associated_file_label"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_toRightOf="@id/upload_button"
        android:layout_alignParentBottom="true"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:text="Associated File"
        android:layout_alignParentRight="true"
        android:textSize="11sp"/>

    <TextView
        android:id="@+id/key_owner_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/upload_button"
        android:layout_above="@id/associated_file_label"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:text="Key Owner"
        android:textSize="11sp" />

    <TextView
        android:id="@+id/url_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/key_owner_label"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/upload_button"
        android:gravity="center_vertical"
        android:text="URL Here"
        android:textSize="16sp" />

</RelativeLayout>
